<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
    </head>
    <body>
        {% for key, message in app.session.getFlashes() %}
        <div class="{{ key }}">
            <strong>{{key}}</strong>: {{ message|trans({}, 'FabsteiShorturlBundle') }}
        </div>
        {% endfor %}
        <div>
            {% block content %}
            {% endblock content %}
        </div>
    </body>
</html>
